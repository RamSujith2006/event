{
  "version": 2,
  "builds": [
    {
      "src": "server-mongodb.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server-mongodb.js"
    },
    {
      "src": "/(.*)",
      "dest": "/server-mongodb.js"
    }
  ],
  "env": {
    "MONGODB_URI": "@mongodb_uri"
  }
}
